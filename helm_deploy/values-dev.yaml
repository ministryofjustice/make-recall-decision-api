---
# Per environment values which override defaults in make-recall-decision-api/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: make-recall-decision-api-dev.hmpps.service.justice.gov.uk

  env:
    APPLICATIONINSIGHTS_CONFIGURATION_FILE: applicationinsights.dev.json
    SENTRY_ENVIRONMENT: DEV
    HMPPS_AUTH_URL: https://sign-in-dev.hmpps.service.justice.gov.uk/auth
    PROBATION_OFFENDER_SEARCH_ENDPOINT_URL: https://probation-offender-search-dev.hmpps.service.justice.gov.uk
    PROBATION_COMMUNITY_API_ENDPOINT_URL: https://community-api-secure.test.delius.probation.hmpps.dsd.io

generic-prometheus-alerts:
  alertSeverity: make-recall-decision-nonprod

gotenberg:
  alertSeverity: make-recall-decision-nonprod
