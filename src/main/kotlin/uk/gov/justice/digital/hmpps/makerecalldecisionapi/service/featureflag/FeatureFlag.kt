package uk.gov.justice.digital.hmpps.makerecalldecisionapi.service.featureflag

// empty enum for now, as we have no behaviours we want to switch on/off yet, but
// the enum definition is required in order for other classes to be set up fully
enum class FeatureFlag(val flagId: String)
